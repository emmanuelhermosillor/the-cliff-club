# The Cliff Club — Cotizador & CRM · variables de entorno
# Copia a .env.local para desarrollo; configúralas también en Vercel (producción).

# --- Supabase (proyecto the-cliff-club) ---
# URL y publishable/anon key. Públicas por diseño (NEXT_PUBLIC_).
NEXT_PUBLIC_SUPABASE_URL=https://ztgkezkygrzwwxrvpckv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_hKNIUzydcAAAiKZ-NkF1dw_bJUcW-fm

# --- Alta de usuarios (opcional, PREPARADO) ---
# service_role SOLO en el servidor — NUNCA en el cliente, NUNCA con prefijo NEXT_PUBLIC_.
# Necesaria únicamente para POST /api/admin/create-user. Déjala vacía si no la usas.
# SUPABASE_SERVICE_ROLE_KEY=
